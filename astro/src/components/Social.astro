---
const { name, url } = Astro.props;
const { default: icon } = await import(
  `../images/icons/${Astro.props.name.toLowerCase()}.svg?raw`
);
---

<a
  href={url}
  class="hover:bg-transparent focus:bg-transparent active:bg-transparent"
  aria-label={name}
>
  <Fragment set:html={icon} />
</a>
